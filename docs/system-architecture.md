# 系统架构

> 参见 [overview.md](overview.md) 返回文档导航

## 系统架构图

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                           EuGraph Process                                    │
├─────────────────────────────────────────────────────────────────────────────┤
│                                                                             │
│  ┌───────────────────┐  ┌───────────────────┐  ┌───────────────────────┐   │
│  │  Metadata Service │  │  Compute Service  │  │   Storage Service     │   │
│  │     (元数据)        │  │     (计算)        │  │      (存储)           │   │
│  └─────────┬─────────┘  └─────────┬─────────┘  └───────────┬───────────┘   │
│            │                       │                        │               │
│            └───────────────────────┼────────────────────────┘               │
│                                    │                                        │
│                        ┌───────────┴───────────┐                            │
│                        │   Service Manager    │                            │
│                        │  (服务配置/启动)      │                            │
│                        └───────────────────────┘                            │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                        Abstract Interfaces                             │  │
│  │  IStorageService / IComputeService / IMetadataService                  │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
│                                    │                                        │
│                   ┌────────────────┴────────────────┐                      │
│                   │                                │                      │
│                   ▼                                ▼                      │
│         ┌──────────────────┐            ┌──────────────────────┐          │
│         │  LocalImpl       │            │  RemoteImpl (RPC)    │          │
│         │  (零开销)         │            │  (fbthrift)          │          │
│         └──────────────────┘            └──────────────────────┘          │
│                                                                             │
│  ┌───────────────────────────────────────────────────────────────────────┐  │
│  │                     WiredTiger KV Store                               │  │
│  └───────────────────────────────────────────────────────────────────────┘  │
└─────────────────────────────────────────────────────────────────────────────┘
```

## 服务定义

### Storage Service (存储服务)

| 职责      | 说明                   |
| ------- | -------------------- |
| 图数据存储   | Vertex / Edge 的 CRUD |
| KV 存储封装 | WiredTiger 接口封装      |
| 本地事务    | 单节点 MVCC 事务          |
| 数据持久化   | WAL + Checkpoint     |

### Compute Service (计算服务)

| 职责     | 说明                                          |
| ------ | ------------------------------------------- |
| Cypher 解析 | ANTLR4 词法/语法分析，生成 AST                      |
| 查询规划   | AST → 逻辑计划 → 物理计划                           |
| 查询优化   | 基于规则/代价优化（预留 `LogicalPlanVisitor` 扩展点）     |
| 查询执行   | 协程管道（Pull-based 火山模型），IO 与计算分离，详见 [查询引擎设计](query-engine-design.md) |

### Metadata Service (元数据服务)

| 职责        | 说明                         |
| --------- | -------------------------- |
| Schema 管理 | Label/EdgeLabel 类型定义       |
| ID 映射     | Label名称↔ID, EdgeLabel名称↔ID |
| 统计信息      | 数据分布、基数估计                  |
| ID 分配     | 全局唯一 ID 生成                 |

## 服务组合模式

| 模式     | 开启的服务                        | 说明                 |
| ------ | ---------------------------- | ------------------ |
| 纯存储节点  | Storage                      | 只负责数据存储            |
| 纯计算节点  | Compute                      | 无状态计算，连接存储节点       |
| 存算一体   | Storage + Compute            | 单机部署，本地计算，零 RPC 开销 |
| 全功能节点  | Storage + Compute + Metadata | 单机全功能 / 分布式协调节点    |
| 纯元数据节点 | Metadata                     | 独立的元数据中心           |
