# AGENTS.md — EuGraph 开发规则

> 本文档是 AI 编码工具（Claude Code、DeepSeek Harness 等）在本仓库必须遵守的规则事实来源。
> 收到任务后，先完成“零-一”检查，再进入“零-三”流程。
> 技术知识入口是 [docs/README.md](docs/README.md)：构建、架构、用法、任务路由都以它为准。

---

## 零、流程红线（BLOCKING）

### 零-一、每次任务启动前（逐项确认后才能改代码）

> 以下每一项都必须在动手前完成。任一项未满足，**禁止修改任何文件**。

- [ ] **分支检查**：当前在 `main` 或 `develop` 上？→ **立即创建新分支**，然后再做任何修改。分支命名见“五、分支与提交规范”。
- [ ] **知识装载**：
  1. 阅读 [docs/README.md](docs/README.md)，了解项目结构；
  2. 使用其中的“任务路由”表对当前任务**分类**；
  3. 阅读该任务对应的**设计文档**和**关键源码**；
  4. 找到与改动相关的**现有测试**。
- [ ] **方案对齐**：在第 1 步方案中必须复述：任务类型、涉及模块、已读文档、相关测试、风险点；修复类任务还必须给出**根因证据**与**同类问题是否被一并覆盖**的说明（见“一、2. 变更范围与修复策略”）。

### 零-二、每次提交前（BLOCKING）

> 以下检查全部通过后才能 `git commit`。具体命令见“四、标准验证命令”。

- [ ] **格式化**：通过
- [ ] **编译**：通过
- [ ] **测试**：通过
- [ ] **文档同步**：代码变更涉及接口/行为/文件组织变化时，已检查并更新相关设计文档？
- [ ] **验证有效性**（见“一、3. 缺陷排查与验证纪律”）：修复类改动是否已用**能区分对错**的判据证明过？新测试是否已确认在缺陷存在时**会失败**？

### 零-三、编码实施流程（BLOCKING — 逐项确认后才能继续下一步）

> 以下步骤必须严格按顺序执行，每步完成后必须等待开发者明确确认才能进入下一步。
> 🔴 **违反上述流程将导致严重问题！**

- [ ] **第 1 步：设计方案对齐** → 等待开发者明确确认 ✅
- [ ] **第 2 步：测试用例对齐** → 等待开发者确认 ✅；纯文档/规则变更也必须由开发者确认本步骤为 N/A
- [ ] **第 3 步：开始编码实现**（前两步确认通过后才能开始）
- [ ] **第 4 步：运行测试**
  - 测试未通过？→ **自行修复问题**，重复第 4 步直到全部通过
  - 测试全部通过后，才能进入第 5 步
- [ ] **第 5 步：通知开发者验证**（测试全部通过后方可通知）

---

## 一、通用原则与排查纪律（所有场景适用）

### 1. 功能保护
- **做任何改动时，必须思考是否会破坏已有功能**
- 如果改动会影响已有功能，**必须先询问开发者**
- 不得擅自破坏或删除已有功能，除非开发者明确要求

### 2. 变更范围与修复策略
- **根因修复优先**：修复 bug 必须先定位并消除根因，从机制/架构层一次性解决同类问题（class-level fix）；禁止只覆盖单个复现用例的补丁式特判。
- **“最小 diff”是范围约束，不是方案选择标准**：当“正确 / 一劳永逸的修复”需要更大改动时，以根因修复为准；“最小 diff”只用于控制改动边界（不顺手重构无关代码、不批量改动无关文件），不得作为选择劣化方案的借口。
- **方案必须说明根因与影响面**：第 1 步方案中要明确：① 根因证据（日志 / 复现 / 测试）；② 同类问题是否被一并覆盖；③ 正确方案与最小补丁方案对比（若放弃大改方案，必须给出理由）；④ 新增的回归测试与文档更新点。
- **同类失败禁止逐条特判**：优先补齐统一机制（作用域解析、SlotId/Layout 传播、子句管线、错误码 / 类型系统等），而不是在单个算子里加局部 workaround；已有统一机制时，修复应落在机制层，并顺带清理由此产生的历史 workaround。
- 禁止为了“顺手”升级依赖、切换子模块版本、批量格式化无关文件
- 禁止删除、弱化或绕过测试用例来让测试通过
- 遇到任务描述与本文档冲突、需求不明确、涉及兼容性行为变更时：**停止并询问，不要猜测**

### 3. 缺陷排查与验证纪律

> 以下每条都来自一次真实的排查教训（十一轮才定位到根因，前九轮的方向全被后续否证）。
> 复盘见 [docs/query/comprehension-defect-debugging-notes.md](docs/query/comprehension-defect-debugging-notes.md)。

- **先定判据，再动手**：动手改代码前，先写下「什么结果能证明我的假设错」。判据必须能**区分正确与错误**（先让它在缺陷存在时失败）。没有判据的改动一律视为未验证。
- **判据必须独立**：不用与缺陷**同源**的机制验证它（用推导验证推导、用同一层算子验证同一层算子）；期望值用**外部实现**核对（Cypher 语义以 neo4j 为准），而不是自己推断。
- **从能复现的最小输入做删减二分**，不要从理论猜机制：保留能复现的最小集合，逐个删元素，定位哪些是必需条件。这通常一次就能否证掉大部分猜测。
- **先证明 fixture 能失败**：新的测试或复现脚本，必须先确认它在缺陷存在时**确实失败**，再用于验证修复。「测试通过」可能什么都没覆盖。
- **「找到真实缺陷」≠「找到成因」**：把局部缺陷当成病因前，先验证修掉它是否**改变目标行为**。同一函数常并存多个独立缺陷 —— 修好若干个而目标行为不变是常见情形。
- **大改动前做单变量隔离**：逐处禁用改动并跑全量测试，避免把**多个改动的合并效果**误判为其中某一个的功效。
- **观测优于推断**：打印**运行期的实际状态**（作用域符号表、列值、算子触发的比较），而不是从计划形状或类型定义推断。选对观测点位（如「子计划构建前的 ctx_.symbols」）通常一步定位。
- **性能结论必须同二进制交错 A/B**：跨时间比较前先确认机器状态（本机 CPU 频率会降到 ~33%，使同一二进制慢约 2 倍），否则会把环境变化误判为代码回归。绝对值只在注明条件下引用。
- **结论要经得起交叉核验**：数据本身先证明等价，再谈引擎差异（见本节第 4 小节）。

### 4. 与 neo4j 对照（Cypher 语义的判定手段）

> neo4j 是本项目 Cypher 语义的**外部参照**。语义类改动与其靠推断，不如**跑一遍对照**。
> 完整用法见 [docs/benchmark/ldbc-snb-sf0.1-comparison.md](docs/benchmark/ldbc-snb-sf0.1-comparison.md)
> 与 `scripts/bench_ldbc_complex7.py`。

- **判定「谁对」时用它能省掉全部争论**：本机 neo4j（7687）与 eugraph（7688）可同时运行，同一条查询两边跑、逐行比结果即可。
- **对照脚本要固化下来**：一次性查询只能回答一次问题；把 fixture 建图 + 查询 + 比对写成脚本（参照 `scripts/bench_ldbc_complex7.py`），后续回归可直接复用。同一查询的多组变体（换谓词、换方向、换顺序）应一次跑完再判读。
- **期望值先对 neo4j，再写断言**：写测试断言前先运行一次 neo4j —— 错的很可能是**自己的期望**，而不是引擎。
- **注意字面量与类型差异**：neo4j 的 `id` 是**字符串**而 eugraph 是整数（对照脚本因此有 `id_type: str/int`）。用错会得到「查不到」的假象；日期等类型的序列化差异同理。
- **数据等价是前提**：对照前先按标签/边类型核验两边的点、边与**查询相关邻域**（见上一条）。已知差异：neo4j 缺少 `Message` / `City` / `Country` / `Continent` / `University` / `Company` 这些**派生细分标签**，故依赖它们的查询（如 complex-10）在 neo4j 上返回空 —— 此时不能用它判定，改用**不含该机制的独立路径**（如普通 `MATCH` + `count(DISTINCT …)`）自行交叉核验。
- **每次测试都从全新的数据开始**：把「清空 → 建图 → 查询」当作一次完整 setup，不要在上一次的残留状态上跑。最小测试图应当先**清掉已有数据再重建**（两边都要），这样重复运行时结果可比，也不会被前一次实验写入的节点/边、或某次中途失败留下的半成品干扰判断。
  - 本仓库的教训：探针曾把 `:X` / `:Y` / `:L` 写进运行中的实例，后续一批读数取自被残留污染的库，据此得出过错误结论。
  - **注意清空手段的代价**：`MATCH (n) DETACH DELETE n` 在**大库**（如 sf0.1 基准库）上会超出 neo4j 的事务内存上限并使 eugraph 挂住。小测试图用「清空后重建」；大库请改用**专用测试库**（建一个新的、用完即弃），不要对共享基准库做破坏性清理。

### 5. 代码与文档规范
- **禁止使用裸指针和取地址符号（`&`）**：除非完全无法绕开，否则一律使用智能指针或引用替代裸指针，禁止对变量取地址
- 遵循项目现有的代码风格，遵循现代 C++ 风格、遵循 C++ Core Guidelines
- 修改前先阅读相关代码，理解现有实现
- **修改代码后，必须检查相关设计文档是否需要同步更新**（如新增接口、变更行为、调整设计等）
- **关键设计决策必须写入专门的设计文档**，防止后续修改破坏已有设计（位置与索引规则见“六、文档同步”）

#### 物理设计与复杂度控制（参考《大规模C++程序设计》）
- **最小化头文件依赖**：优先使用前置声明，仅在实际使用定义时才 `#include`（如只需指针/引用时无需引入完整定义）
- **编译防火墙**：对内部实现细节使用 Pimpl 或纯接口类隔离，减少头文件暴露，降低编译耦合
- **组件单一职责**：每个 `.h/.cpp` 文件都围绕一个明确的职责，不要把不相关的功能混在同一个文件中
- **依赖方向单一**：模块间依赖必须单向，禁止循环依赖；高层依赖低层，低层不感知高层
- **避免过度抽象**：不要为假设的未来需求设计，三个相似场景再抽象，两个就直接写
- **头文件自包含**：每个 `.h` 文件必须能独立编译，所需依赖自行 include，不依赖调用方的 include 顺序

---

## 二、图数据库项目技术红线

- **兼容性表面**：Bolt 线协议、KV 编码、Thrift 字段编号/类型、已有 Cypher 语义属于外部兼容性表面。修改前必须阅读对应设计文档，并在方案中说明兼容性影响，取得开发者确认。
- **生成代码禁止手改**：
  - `src/query/parser/generated/grammar/*`：修改 `.g4` 后运行 `./scripts/gen-grammar.sh`；
  - `src/service/thrift/gen-cpp2/*`：修改 `proto/eugraph.thrift` 后按 [docs/build/build-guide.md](docs/build/build-guide.md) 重新生成；
  - 生成代码出现 diff 时必须核对生成步骤，不得手工修补生成结果。
- **测试纪律**：查询语义/Bolt/KV 编码变更，除单元测试外，按需运行 TCK 或驱动兼容性验证；TCK 基线变更必须先取得开发者确认，驱动兼容性结论不得虚报。
- **性能影响**：涉及扫描范围、索引选择、批处理方式或并发行为的改动，必须在方案中说明预期影响；具体性能模式以对应设计文档为准。

### Binder 身份模型（修改 Binder/DPL 时强制遵守）

> 完整不变量清单见 `docs/query/engine/pattern-join-planner-design.md` 第 4.4 节。
> 以下只保留跨模块红线，细节不得在本文件重复维护。

- **`VariableId = SlotId`**：语义身份是 Binder 分配的绑定槽；same slot = same semantic binding，different slot = different semantic binding。禁止以变量名或 `ScopeId + name` 作为语义身份。
- **`ScopeId` 只用于可见性/provenance**：不是 slot identity 的第二来源；`save/restore` 必须恢复作用域信息。
- **标签顺序是 presentation metadata**：不得写入 `VertexValue` 或存储编码。
- 任何身份模型变更必须同步 `docs/query/engine/pattern-join-planner-design.md`。

---

## 三、安全策略

- 不得提交、记录或输出任何密钥、令牌、凭证、私有连接串。
- 不得弱化认证、授权、数据隔离、传输加密或审计行为。
- 新增第三方依赖、网络行为、进程权限变化必须先取得开发者确认。
- 对用户输入和外部数据（Cypher 文本、Bolt 数据包、导入文件）的解析变更，必须考虑非法/超长/畸形输入的失败行为，不得静默接受或越界访问。

---

## 四、标准验证命令

> **优先走 skill**：格式化调用 `eugraph-format`；构建 / 测试 / 专项验证调用 `eugraph-verify`。
> 两个 skill 位于 `.agents/skills/<name>/SKILL.md`，DeepSeek Harness 会从项目根目录 `.agents/skills` 自动发现。
> skill 不可用时，按下面命令兜底执行。

### 默认验证（提交前必须全部通过）
```bash
# 1. 格式化（自动修复后重新检查应通过）
./scripts/check-format.sh --fix
./scripts/check-format.sh

# 2. 构建（默认 debug；如开发者指定其他 preset 则用指定项）
cmake --preset debug        # 首次或 CMake 配置变更时执行
cmake --build --preset debug

# 3. 先运行与本次改动直接相关的测试（示例，按任务替换实际测试名）
./build/debug/query_executor_tests --gtest_filter='*<RelatedTest>*'

# 4. 再运行全量 ctest
ctest --preset debug --output-on-failure
```

### 特定变更
- **修改 `grammar/*.g4`**：`./scripts/gen-grammar.sh`，并按 [docs/build/build-guide.md](docs/build/build-guide.md) 校验生成文件。
- **修改 `proto/eugraph.thrift`**：按 [docs/build/build-guide.md](docs/build/build-guide.md) 重新生成 gen-cpp2 代码，并校验 include 路径。
- **查询语义/Bolt/编码变更**：按 [docs/tests/tck-guide.md](docs/tests/tck-guide.md) 运行 TCK 验证；不得私自更新基线。

> 所有验证命令必须真实执行并汇报实际输出；**禁止假装命令已运行**。

---

## 五、分支与提交规范

- 禁止直接在 `main` / `develop` 上提交。
- 分支命名建议：
  - `feature/<topic>`
  - `fix/<topic>`
  - `docs/<topic>`
  - `refactor/<topic>`
- 提交信息建议参考仓库历史：
  - `feat(scope): ...`
  - `fix(scope): ...`
  - `docs(scope): ...`
  - `refactor(scope): ...`
  - `chore(scope): ...`
- 只有完成“零-二”全部检查后才允许提交；未取得开发者明确指示，不推送分支。

---

## 六、文档同步

- 关键设计决策必须落入 `docs/<domain>/<topic>.md`（kebab-case 命名）。
- 新增设计文档后，必须同步更新 [docs/README.md](docs/README.md) 的导航或“任务路由”表。
- 接口、行为、文件组织变化时，必须同步修改既有设计文档，避免文档与实现漂移。
- 本文档只写跨任务通用规则；模块技术细节写入对应设计文档，不重复堆进 AGENTS.md。
